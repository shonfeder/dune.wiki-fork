# Compile-time toggling of feature flags

 - unifying the `DUNE_CONFIG__` variables with compile-time configuration
 - changing the default config variable values for the dev preview
 - coordinating changes to dune_config/config.ml between Jane Street and Tarides