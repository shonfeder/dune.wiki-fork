## Agenda

- Discuss the lock files (`dune.lock` and `dev-tools.lock`): where should they live? (see [this issue](https://github.com/ocaml/dune/issues/10955))
- To be able to use `dune pkg` with dune we need to fix https://github.com/ocaml/dune/issues/10855. What's the most sensible thing to do?
- Dune release cycle: roadmaps, plans, when to do it, etc?